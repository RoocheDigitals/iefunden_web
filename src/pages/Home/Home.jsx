import { useNavigate } from "react-router-dom";
import { Box, Heading, Stack, Text } from "@chakra-ui/react";

const Home = () => {
  const navigate = useNavigate();

  return <Box bgColor="red">Hello</Box>;
};

export default Home;
